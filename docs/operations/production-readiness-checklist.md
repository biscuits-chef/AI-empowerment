# 生产就绪检查清单

每个适用条目都必须记录负责人和证据链接。标记 `N/A` 时必须说明原因。任何未完成的关键条目都会阻塞发布。

## 当前评估（2026-09-08）

结论：**NO-GO / 未达到生产发布条件**。本次交付收紧了问题提交幂等指纹、追问短路、逐实体来源消息 ID、非法日期校验、未映射业务字段、HiAgent 流契约启动门禁、取消任务最大尝试次数与错误响应安全性；这些改动只证明本地失败关闭能力。真实 GoldenDB 迁移与查询计划、产品数据权限、独立只读数据源、确定性字段级对账、双通道证据快照与引用、持久生成任务、跨实例 Redis、真实 OBS、统一认证以及生产 SLO 仍未完成。公司知识库与 HiAgent 的真实契约仍为发布阻塞项。

本地代码证据：2026-09-08 在 OpenJDK 8u452、Maven 3.9.9 下执行 `mvn -B -ntp clean verify`，158 项测试通过，0 失败、0 错误、0 跳过；Checkstyle、PMD/CPD、SpotBugs、ArchUnit、中文 Javadoc、JaCoCo、Enforcer 和 Failsafe 均通过。JaCoCo 报告覆盖 80 个纳入统计的类，指令覆盖率 90.65%，分支覆盖率 73.70%。生成 JAR 的 SHA-256 为 `16b2a4be03c615407c56848855fa4f255633a09cea789718b844056ac0651942`。H2 MySQL 模式测试仍使用手写 Schema 并关闭 Flyway；仓库目前没有命名匹配 `*IT` 的 Failsafe 集成测试，JaCoCo 也排除了适配器、配置和通用包。因此本地门禁通过不能替代真实 GoldenDB、Redis、OBS、HiAgent、企业身份平台和多副本环境验证。

## 产品与责任归属

- [ ] 需求和验收标准已经批准，且不存在影响发布的 `TBD`。
- [ ] 已记录服务负责人、值班轮值、升级路径、代码仓库、服务目录条目和仪表盘。
- [ ] 已记录依赖、使用方、数据负责人和支持边界。

## 架构与质量

- [x] 当前交付目录上的 `mvn -B -ntp clean verify` 已通过；正式发布提交需再次执行。
- [x] Checkstyle、PMD/CPD、SpotBugs、ArchUnit、JaCoCo、单元与 MVC 门禁均通过；真实环境集成测试仍待完成。
- [x] 已使用内嵌 Jetty 替代 Tomcat Web 服务器；Maven 禁止 Tomcat Starter 和全部嵌入式 Tomcat 组件，完整应用测试会启动 Jetty 验证装配。
- [x] `dev`、`test`、`prod` XML 配置已分离，启动校验禁止缺失、错配或同时启用多个环境 Profile；生产演示模式固定关闭且无默认凭据。
- [ ] 相关 ADR 已接受；已知技术债有明确负责人和完成日期。
- [ ] 容量假设和限制来自实际度量，而非猜测。

## GoldenDB 与数据安全

- [ ] 已确认 GoldenDB 产品版本、拓扑、兼容模式、分片 Key 和全局索引。
- [x] 应用数据访问已切换为 MyBatis-Plus 3.5.17；单表 CRUD、Owner 隔离、逻辑删除、消息顺序、问题提交幂等指纹、反馈 Upsert、回答终态同步和 30,000 个中文字符内容容量具备 H2 测试。停止等其他操作尚未统一落地 `(Owner, operation, key, fingerprint)` 记录。
- [x] 一期产品查询使用固定 MyBatis Statement；H2 测试覆盖最新快照、实体解析、两种投资经理口径、两个日期条件和结果上限。目录中缺少物理映射的字段会在 SQL 前失败关闭，但真实字段字典仍待数据负责人批准。
- [ ] 数据平台已签署并同步 `dws_product_info_d` 完整快照，确认三个日期字段质量规则、产品数据权限和失败批次处理方式。
- [ ] 业务查询使用独立只读账号与连接池，生产 `app.business-query.enabled` 在完成真实环境门禁前保持关闭。
- [ ] 在真实 GoldenDB 验证 MyBatis-Plus 生成 SQL、XML 联表 SQL、`ON DUPLICATE KEY UPDATE`、`TIMESTAMP(6)` 和异常转换行为。
- [ ] `V3` 已按前向迁移扩展 `qa_message.content`，但仍需在真实 GoldenDB 用代表性数据验证语法、在线 DDL、锁表时间及前滚方案。
- [ ] `V4` 新增 `qa_conversation_context`，仍需在真实 GoldenDB 验证主键/Owner 索引、`MEDIUMTEXT` JSON 容量、乐观版本并发和滚动发布兼容性。
- [x] `V5` 临时文件元数据、处理状态、Owner/会话隔离和问题文件用途关联具备 MyBatis-Plus H2 集成测试。
- [ ] `V5` 已在真实 GoldenDB 验证 SQL 方言、索引、滚动发布兼容性和前滚方案。
- [ ] `V7` 已在真实 GoldenDB 验证 `dws_product_info_d` 复合主键、索引长度、字符集、迁移耗时和前滚方案。
- [ ] 关键 SQL 具备真实 GoldenDB `EXPLAIN` 证据，结果集有明确上限。
- [ ] 连接池规模和超时符合所有副本共享的数据库连接预算。
- [ ] 隔离、锁、死锁重试、幂等性和故障切换行为均已测试。
- [ ] 备份、时间点恢复、还原、RPO、RTO、保留和灾难恢复能力均有证据。
- [ ] 数据分类、加密、脱敏、删除、保留和审计控制已批准。

## Redis

- [ ] Redis 的角色、拓扑、持久化、TLS/认证、内存限制和淘汰策略均已明确。
- [ ] 每个 Key 都带命名空间和版本，并有合理的 TTL 或永久保存决策。
- [ ] 缓存击穿、热点 Key、大 Value、故障切换及损坏值行为均已测试。
- [ ] 当 Redis 不是事实来源时，Redis 不可用期间服务仍保持正确性。

## OBS

- [x] 开发环境具备受限本地对象存储替身，上传接口覆盖 1 MiB、数量、扩展名、内容类型、文件头、摘要、幂等和问题引用校验；该替身不用于生产。
- [ ] Bucket 区域、私有访问、IAM 策略、网络路径、TLS、加密/KMS、版本控制和生命周期已批准。
- [ ] 凭据来自密钥管理系统，且轮换流程已测试。
- [ ] 大小/类型校验、恶意软件策略、校验和、分段阈值、重试/超时和孤儿对象清理均已测试。
- [ ] 跨系统部分失败与对账具备可观测且责任明确的处理机制。
- [ ] 数据驻留、保留、法律保全、复制、恢复和删除行为已批准。

## 可靠性与性能

- [ ] 可用性、延迟、错误、吞吐量、RPO 和 RTO 的 SLO 已批准，并包含错误预算策略。
- [ ] 公司知识库平台查询接口完成契约测试；认证、权限透传、超时、限流、空结果、越权拒绝和故障恢复行为均有证据。
- [ ] 以持续 10 QPS 问答提交量完成不少于 30 分钟的真实依赖压测；容量按 `10 × P95 完整回答秒数` 计算并额外保留至少 30% 余量。
- [ ] 负载与浸泡测试覆盖峰值 RPS、并发量、上传大小分布，并保留至少 30% 容量余量。
- [ ] 每个远程调用都设置超时；重试有次数上限、随机抖动、故障分类和幂等保证。
- [ ] 舱壁/并发限制和背压可防止级联故障。
- [ ] GoldenDB、Redis、OBS、DNS 和网络故障测试能够证明预期降级与恢复行为。
- [ ] 至少两个生产副本跨故障域部署；中断与自动扩缩容策略已经测试。
- [ ] 停止任务已持久化，租约过期领取受最大尝试次数约束，最后一次领取后崩溃可在租约到期时原子收敛为失败，且启动保护要求租约长于模型连接与读取超时；仍缺少续租/Fencing、抖动退避、独立执行舱壁及真实多副本和 `/stop_message` 幂等语义验证。
- [x] 回答阶段、引用产物与人工复核事件先写入 `qa_answer_event`，刷新后可随历史消息恢复；会话列表采用稳定游标，活动会话删除返回固定 `409` 提示。
- [ ] 持久生成任务/Outbox 与跨实例 Redis Stream 已实现，能够在进程重启后继续生成并在任一副本即时续流。
- [ ] 已在多副本和进程重启场景验证停止任务恢复、重复投递、租约过期与最终一致性。

## 问答准确性与上下文

- [ ] 产品/交易基础信息和产品最新信息均有经业务签署的意图定义、字段字典、别名与标注集。
- [ ] 意图分类准确率、实体抽取准确率、检索 `Recall@K`、冲突召回率和事实有据率阈值已批准并达标。
- [ ] 每项事实均可追溯到知识片段或受控查询结果；发布黄金集中无证据事实断言为零。
- [ ] 双通道冲突样本均展示两个通道的数据、来源时间和人工复核提示，不由模型静默选择来源。
- [ ] “最新”文档按照版本、发布/生效日期判断，并验证旧版高相关度不能覆盖有效新版。
- [x] 意图低置信度、指代缺失和实体歧义可返回持久化追问，不调用知识库、业务库、对账或模型；用户选择候选后恢复原意图的服务测试已通过。
- [x] 结构化实体按实体保存来源用户消息 ID，并持久化上一意图、待追问候选和乐观版本；旧 JSON 缺少逐实体来源时失败关闭，不从全局问题 ID 猜测来源。H2 测试覆盖 Owner 隔离和跨仓储实例恢复。
- [ ] 回答终态与结构化上下文更新尚未纳入同一事务或可靠事件闭环；需补充事务边界、失败补偿和故障注入测试，避免回答已完成但上下文未更新（或相反）。
- [ ] 生产意图识别器、产品/交易授权候选目录、别名标准化和真实字段字典已接入并通过越权与歧义评测。
- [ ] Token 自适应摘要、超过上下文窗口的主题切换、返回旧主题及跨服务重启端到端测试通过。

## 公司 HiAgent 模型接口

- [ ] `PROD_COMPANY_MODEL_BASE_URL` 指向目标智能体的 `/api/v1` 前缀，网络、DNS、TLS 和访问控制已经验证。
- [ ] 智能体创建会话所需 `Inputs`、模型版本和发布版本已经确认并固定。
- [ ] SSO 用户标识满足 `UserID` 1～20 字符要求，或已批准稳定映射方案。
- [ ] 已取得真实脱敏 `chat_query` 流事件样例，明确答案、结束、错误、`MessageID`、`TaskID` 和用量字段，并固化契约测试。
- [x] 未确认流契约时默认禁止启用 HiAgent；本地解码器只读取文档当前明确的根级 `Answer` 与 `MessageID`，不递归搜索或兼容猜测字段。
- [x] SSE 单行和单事件不得超过 `COMPANY_MODEL_MAX_REQUEST_CHARACTERS`，单次响应累计字符不得超过该值的四倍；超限在 JSON 解析前失败关闭。真实平台容量仍需联调后批准。
- [ ] 创建会话和流式聊天的字段名、大小写、Content-Type、超时、空流、畸形事件及非 2xx 测试全部通过。
- [x] 本地出站契约测试已验证 `/stop_message` 使用文档规定的 `UserID` 与 `MessageID` 字段。
- [ ] 在真实平台验证流事件 `MessageID` 路径及停止成功、重复停止、已完成消息、限流、超时与非 2xx 语义。
- [ ] 平台限流、连接上限、请求字符/Token 上限、重试约束和 10 QPS 容量已经确认。
- [ ] 模型输入输出留存、训练使用、审计、内容安全和数据跨域策略已经批准。

## 安全

- [x] 开发环境模拟用户由服务端固定配置注入，不采信浏览器身份；测试和生产启用模拟认证时拒绝启动。
- [ ] 公司统一认证页面跳转、回跳、身份声明和会话失效行为已经联调；一期不建设自有登录页。
- [ ] 认证、授权、租户隔离、滥用场景和威胁模型已经审查。
- [ ] 代码、镜像、日志、指标、Trace 和错误响应中均不存在密钥或敏感数据。
- [x] Web 问题响应包含稳定 `code` 和 `traceId`，解析、持久化及未知异常使用固定安全中文说明，不回显底层异常消息；`traceId` 与日志链路的关联仍待实现。
- [ ] SAST、依赖、许可证、密钥、容器和 SBOM 检查通过，并具备例外审批流程。
- [ ] 镜像体积最小、版本固定、使用非 root 用户与只读文件系统，并移除不必要能力。
- [ ] 外部端点、CORS、速率限制、载荷限制和 Actuator 访问均已加固。

## 可观测性与运维

- [ ] 结构化日志包含时间戳、严重级别、服务/版本、Trace ID 和安全的诊断上下文。
- [ ] RED 指标、JVM、HikariCP、GoldenDB、Redis、OBS、业务和补偿指标均有仪表盘。
- [ ] 存活、就绪、启动和优雅停机行为均已测试。
- [ ] 症状驱动的告警具备已测试的阈值、负责人、去重策略和运行手册链接。
- [ ] 运行手册覆盖依赖中断、资源饱和、迁移失败、数据不一致、凭据过期和回滚。

## 发布与回滚

- [ ] 已记录不可变制品的来源、签名/校验和、环境晋级流程和配置负责人。
- [ ] 发布平台已分别配置 `TEST_*`、`PROD_*` 变量和密钥引用，并通过配置差异审查证明不存在跨环境地址或凭据串用。
- [ ] 金丝雀/滚动策略、健康门禁、功能开关和停止标准已经演练。
- [ ] 应用回滚与已部署表结构兼容；否则前滚路径已经演练。
- [ ] 部署后验证覆盖业务行为与 SLO。
- [ ] 变更窗口、沟通安排、事件指挥官和发布/停止决定权限均已指定。

## 签署

| 角色 | 姓名 | 决策 | 日期 | 证据 |
|---|---|---|---|---|
| 产品负责人 | TBD | TBD | TBD | TBD |
| 研发负责人 | TBD | TBD | TBD | TBD |
| 运维/SRE | TBD | TBD | TBD | TBD |
| 安全负责人 | TBD | TBD | TBD | TBD |
| 数据/DBA | TBD | TBD | TBD | TBD |
