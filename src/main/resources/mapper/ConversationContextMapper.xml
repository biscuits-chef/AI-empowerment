<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acme.intelligentqa.adapter.out.persistence.mybatis.ConversationContextMapper">

    <resultMap id="conversationContextResult"
            type="com.acme.intelligentqa.adapter.out.persistence.mybatis.ConversationContextPersistenceRecord">
        <id property="id" column="id"/>
        <result property="conversationId" column="conversation_id"/>
        <result property="ownerId" column="owner_id"/>
        <result property="version" column="version"/>
        <result property="stateJson" column="state_json"/>
        <result property="sourceQuestionId" column="source_question_id"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert"
            parameterType="com.acme.intelligentqa.adapter.out.persistence.mybatis.ConversationContextPersistenceRecord"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO qa_conversation_context
            (conversation_id, owner_id, version, state_json, source_question_id, updated_at)
        VALUES
            (#{conversationId}, #{ownerId}, #{version}, #{stateJson}, #{sourceQuestionId}, #{updatedAt})
    </insert>

    <select id="selectByOwnerAndConversation" resultMap="conversationContextResult">
        SELECT id, conversation_id, owner_id, version, state_json, source_question_id, updated_at
        FROM qa_conversation_context
        WHERE conversation_id = #{conversationId}
          AND owner_id = #{ownerId}
    </select>

    <update id="updateByVersion">
        UPDATE qa_conversation_context
        SET version = #{record.version},
            state_json = #{record.stateJson},
            source_question_id = #{record.sourceQuestionId},
            updated_at = #{record.updatedAt}
        WHERE conversation_id = #{record.conversationId}
          AND owner_id = #{record.ownerId}
          AND version = #{expectedVersion}
    </update>

</mapper>
