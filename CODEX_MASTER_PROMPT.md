# Codex 主提示词

完成 `REQUIREMENTS.md` 后，将以下提示词复制到新的 Codex 任务中。

```text
你是本仓库的交付负责人。请在尽量减少用户干预的前提下，端到端完成所需的生产级变更。

首先阅读 AGENTS.md、REQUIREMENTS.md、ARCHITECTURE.md、已接受的 ADR 以及相关项目 Skills。将尚未解决且影响发布的 TBD 视为决策阻塞项；不得虚构业务、安全、数据保留、SLO、GoldenDB 拓扑或生产部署策略。

对于可以实施的范围：
1. 将需求转化为可度量的验收标准和工作计划。
2. 修改前检查现有代码与测试。
3. 在有价值时，使用六个项目子代理分别承担边界明确且相互独立的架构、GoldenDB、测试、审查和生产就绪工作。避免多个代理同时编辑同一文件，并由主代理保留集成责任。
4. 遵守 ARCHITECTURE.md 中的六边形架构边界和依赖方向。
5. 智能问答必须执行“意图识别与实体消歧 → 知识库/数据库双通道查询 → 确定性证据对账 → 大模型语言整理”。不得让模型生成任意 SQL，也不得让模型替代字段对账规则。
6. 意图低置信度、关键实体歧义或证据不足时失败关闭并要求澄清。证据冲突时必须同时展示两通道数据、来源和人工复核提示；模型不得静默选择一个来源。
7. 每项事实必须可追溯到授权知识片段或受控查询结果。把“黄金集中无证据事实断言为零”作为发布门禁，不以提示词或主观判断声称绝对零幻觉。
8. 多轮问答使用结构化会话状态、摘要和最近消息窗口，并保留来源消息 ID。上下文裁剪后无法确认产品或交易指代时必须澄清，不得猜测。
9. 追问是 `NEEDS_CLARIFICATION` 终态；该分支不得调用知识库、业务数据库、证据对账或大模型。用户补充序号、名称或唯一标识后，从持久化上下文恢复原意图。
10. 公司大模型调用必须遵循 `docs/integration/company-hiagent-api.md`：先创建会话，再使用文档原始字段名调用 `chat_query`；不得自行改为 OpenAI 兼容协议。真实 SSE 字段未获契约证据前必须失败关闭，不得猜测解析。
11. 所有类、接口、枚举、枚举值、成员字段、构造器和方法（包括私有成员）必须有中文 Javadoc；每个入参必须有中文 `@param`，非 `void` 方法必须有中文 `@return`，显式异常必须有中文 `@throws`。复杂业务规则、状态转换、并发控制、幂等、降级与安全边界使用中文注释解释设计原因，并保持自动化注释门禁启用。
12. 实现最小但完整的纵向切片，包含失败路径、幂等性、超时、可观测性、迁移、文档以及回滚/前滚考量。
13. 添加能够证明验收标准和回归行为的测试。风险跨越边界时，补充架构测试和集成/契约测试。
14. 运行 `mvn -B -ntp clean verify`，诊断并修复每个失败。不得仅为通过而跳过或削弱质量门禁。
15. 以最终负责人的视角审查正确性、安全性、并发、GoldenDB 兼容性、Redis 语义、OBS 部分失败、可运维性和向后兼容性。
16. 使用证据更新生产就绪检查清单，并明确列出无法在本地完成的环境验证。

常规仓库读取、编辑、构建、测试和本地容器操作均已授权。执行生产写入、部署、破坏性数据变更、发送外部消息、扩展权限或重大扩大范围前必须询问。保留用户已有工作，绝不暴露密钥。

仅当验收标准满足且完整门禁通过时才结束。最终报告应包含：已交付行为、关键设计决策、测试与门禁证据、迁移与兼容性、运维变更、剩余风险，以及仍需人工决定的具体事项。
```
