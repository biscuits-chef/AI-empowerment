# AI 赋能项目第一阶段需求说明

> 当前技术代号为 `intelligent-qa-audit-service`，正式服务名称待确认。本文件以飞书《产品需求文档》及后续确认结论为需求基线，统一工作台交互以飞书文档为主、以后续明确确认覆盖冲突表述。标记为 `TBD` 的产品、安全、接口或环境决策不得由代码自行推断；尚未完成真实环境联调的能力不得描述为生产可用。

## 1. 文档状态与已确认决策

以下结论已经确认，后续设计和开发必须遵守：

| 主题 | 已确认结论 |
|---|---|
| 第一阶段范围 | 聚焦智能问答，同时提供统一工作台所需的最小公共能力。 |
| 用户 | 公司内部用户。 |
| 统一认证 | 第一阶段必须接入公司统一认证；未认证用户先跳转公司统一认证页面。是否建设本系统登录页留待后续处理，一期不建设、不模拟公司登录页。开发环境保留模拟用户，并通过配置开关切换。 |
| 模拟认证 | 只允许开发环境使用；测试和生产环境配置为模拟认证时必须拒绝启动。 |
| Agent | Agent 名称、顺序和开放状态由前端本地配置；输入区第一阶段默认且仅开放“智能问数”，其稳定类型为 `SMART_DATA`。后端不提供 Agent 列表接口，只校验并路由提问携带的 `agentType`。 |
| 查询场景 | 后端使用带版本的场景计划编排可复用节点；第一阶段只注册并开放 `DUAL_CHANNEL_QA`，纯 LLM、制度库及其他场景暂不支持。 |
| 未开放功能 | 用户选择未开放入口时提示“该功能尚未开放”，不得创建任务或调用执行接口。 |
| 首问建会话 | 用户点击“新建聊天”只进入空白编辑态；第一次有效提问时才创建会话并落库，然后提交问题。 |
| 活动会话删除 | 存在执行中回答的会话禁止删除，返回 `409`，页面提示“会话正在执行，请停止后删除”。 |
| 企业知识库 | 上传、维护、解析、切片和索引由公司知识库平台负责，本系统只调用查询接口。 |
| 业务数据 | 数据中台把产品主题每日全量快照同步到 GoldenDB 表 `dws_product_info_d`，本系统使用 MyBatis-Plus 直接执行受控只读查询。 |
| 业务查询元数据 | 《语义层配置_产品主题数据集_v1.0》中的数据集注册、字段注册和枚举注册只用于帮助模型确定数据集、字段、筛选/分组能力、同义词和枚举值；最终 SQL 查询 `dws_product_info_d`。 |
| 产品日期字段 | `PROD_MAT_DT`（产品到期日期）、`PERI_OPEN_BASE_DT`（定开基准日期）和 `DT`（分区日期）均为 `VARCHAR(10)`，只允许 `yyyy-MM-dd`。 |
| 投资经理口径 | 查询投资经理时同时返回 `INVS_MNGR_NM`（监管口径）和 `PRDC_INVS_MNGR`（产品部口径），两个口径不同不直接判定为数据冲突。 |
| 数据库安全 | 禁止大模型生成任意 SQL 并直接执行；一期由业务语义目录生成受控 Query Plan，再编译为固定参数化 MyBatis 语句。 |
| 临时文件 | 第一阶段支持用户在会话内上传临时文件问答，文件既可作为查询条件/问题补充，也可作为回答证据，但不进入公司知识库。 |
| 文件大小 | 单个文件不超过 1 MiB；单次数量、单会话总容量和保留期为 `TBD`。 |
| OCR | 第一阶段支持扫描 PDF 和图片 OCR。 |
| OBS | 临时文件保存到私有 OBS；允许 HiAgent 使用短时效、只读 OBS 签名 URL 读取指定文件。 |
| 大模型 | 使用公司 HiAgent 运行态 API。 |
| 证据来源 | GoldenDB、公司知识库，以及确实承载事实内容的用户临时文件；仅用于提供产品编号等查询参数的文件不是证据来源。 |
| 冲突处理 | 多来源不一致时必须分别展示数据、来源和时间，并提示人工复核，不得由模型自行裁决。 |
| 容量 | 同时满足持续 10 QPS 问答提交量和至少 50 个并发用户。 |
| Web 运行时 | 使用内嵌 Jetty，不使用 Tomcat 或外部应用服务器。 |
| 环境配置 | `dev`、`test`、`prod` 使用不同 XML 配置文件。 |
| 代码注释 | 类、接口、枚举、字段、构造器、方法和全部入参必须具有准确的中文注释。 |

## 2. 产品目标

建设面向公司内部用户的 AI 工作台。第一阶段通过自然语言理解用户问题，解析临时文件中的补充条件或事实内容，在权限范围内查询公司知识库和 GoldenDB，执行确定性证据对账后调用 HiAgent，流式返回有来源、可追溯、可停止、可追问的回答。

总体产品还包括合同智能比对、申赎确认单处理和合同智能审核，但这些专项业务不在第一阶段实现范围内。

## 3. 第一阶段范围

### 3.1 统一工作台最小能力

- 接入公司统一认证，并在开发环境提供受控模拟用户。
- 查询当前用户、部门、角色、功能权限、知识权限和数据权限。
- 按前端本地配置展示 Agent 入口及可用状态；不依赖后端目录接口。
- 新建、查询、切换、改名和删除会话；会话列表使用稳定游标分批加载，长列表只渲染可见区及少量缓冲行。
- 点击“新建聊天”时不立即写库；第一次有效提问先创建会话并落库，创建成功后才提交问题。空白草稿退出后不产生空会话。
- 会话存在 `PENDING`、`RETRIEVING`、`QUERYING`、`GENERATING` 或 `CANCEL_REQUESTED` 回答时禁止删除，服务端返回 `409`；页面必须原样提示“会话正在执行，请停止后删除”。用户停止并取得明确终态后方可再次删除。
- 首次提问后自动生成会话名称；失败时使用首问安全截断作为备用名称。
- 会话按“今天、昨天、更早”分组展示。
- 保存用户消息、助手回答、任务状态、来源、反馈和执行结果。
- 刷新或重新进入会话时恢复已持久化的执行阶段、来源/产物、人工复核提示和回答状态；活动回答继续从最后事件序号订阅，不能只恢复答案正文。
- 一个会话不得混入其他会话的消息、文件、上下文或任务状态。

### 3.2 智能问答交互

- 发送问题并通过 SSE 流式展示回答。
- 展示意图识别、检索、数据库查询、文件查询、证据对账和模型生成等阶段。
- 支持停止生成，并保留停止前已经生成的文本。
- 支持重新生成；其业务语义是把原问题作为新的问答轮次重新发起，创建新的问题 ID 和回答 ID，继承原问题已经固化的附件用途，重新执行意图识别、文件参数解析、双通道查询、证据对账和模型生成。新轮次与原回答建立追溯关系，旧问题和旧回答均不得覆盖。
- 用户问题受理后，随问题提交的附件必须作为该条用户消息的一部分展示；刷新或重新进入会话时必须从 GoldenDB 恢复，不依赖浏览器临时状态。
- 历史消息只返回附件的文件 ID、安全文件名、内容类型、大小、本次用途和当前状态；不得返回对象键、OBS 签名 URL 或本地路径。附件过期或逻辑删除后仍保留原问题的元数据展示，并明确标记不可用状态。
- 支持复制、喜欢、不喜欢和取消反馈。
- 支持断线重连；事件缺口必须回退 GoldenDB 回答快照，不得静默拼接残缺答案。
- 浏览器断开 SSE 不等于停止生成，只有显式停止接口可以取消任务。

### 3.3 问题理解与上下文

- 识别业务意图、查询指标、维度、时间范围、筛选条件、排序方式和数量限制。
- 提取产品名称、产品编码、交易流水号、文档类型等实体。
- 支持产品简称、别名和受权候选定位。
- 支持“它”“该产品”“这笔交易”等指代消解。
- 缺少必要参数、指代无法证明、低置信度或存在多个候选时返回确定性追问。
- 用户补充名称、唯一标识或候选序号后恢复原意图，不要求重新输入完整问题。
- 用户明确提出新问题时放弃未完成的旧追问。
- 结构化上下文必须持久化，不能只依赖最近若干条原始消息。

### 3.4 问答业务域

第一阶段支持以下业务范围：

1. **产品及交易基础信息问答**
   - 产品经理、投资经理。
   - 产品形式、状态、成立/起息/到期等日期。
   - 当前和历史费率。
   - 产品分层和费率调整计划。
   - 根据单笔交易流水查询交易员。
   - 在批准的指标、维度和枚举范围内执行产品排序或汇总查询。
2. **产品最新信息问答**
   - 最新产品说明书。
   - 费率调整公告。
   - 产品备案通知书。
   - 产品最新状态和动态。
   - 最新版本必须依据版本号、发布日期、生效日期、失效日期或平台提供的等价字段判断，不能依赖模型常识或仅依据检索相关度。

当前产品主题物理表已确认为 `dws_product_info_d`，其数据集、字段、枚举和别名以已发布语义层配置为准。产品主题表的数据权限策略、GoldenDB 环境和生产只读账号仍为 `TBD`。

一期数据库查询采用受控业务语义模型：产品和交易是允许查询的业务实体，产品代码、产品名称、产品经理、投资经理、产品形式、产品状态、起止日期、管理费率、产品分层、费率调整计划、交易流水号、交易员和交易日期是首批标准字段。自然语言只转换为这些标准实体和字段，不允许产生物理表名、列名、Join 或 SQL。

一期 Query Planner 允许生成 `PRODUCT_LOOKUP`、`PRODUCT_REFERENCE_DATE_LIST` 和兼容既有交易能力的 `TRADE_LOOKUP`。产品 SQL Compiler 只允许选择查询 `dws_product_info_d` 的固定参数化 MyBatis 语句；日期列表同时匹配 `PERI_OPEN_BASE_DT` 和 `PROD_MAT_DT`。自由跨表 Join、复杂聚合指标、排名、趋势和任意取数不在一期范围内。

### 3.5 临时文件问答

- 文件只归属于上传用户和指定会话，不进入公司知识库，也不跨会话共享。
- 临时文件具有两种业务角色：
  - `QUERY_INPUT`：作为用户问题的补充条件或批量参数，例如 Excel 中的一批产品编号、交易流水号或筛选条件。
  - `EVIDENCE`：文件正文包含可能支撑回答的事实内容，需要进入证据标准化、引用和冲突检测。
- 系统可使用 `AUTO` 自动识别文件角色，但必须保存识别结果和依据；无法可靠判断时应向用户确认，不能默认把查询参数文件当成事实证据。
- 同一文件可以同时产生查询参数和证据内容，但两类产物必须分开保存、分开授权和分开进入后续流程。
- 一期目标格式为文本 PDF、扫描 PDF、DOCX、XLSX、TXT、MD、JPG、JPEG 和 PNG；旧版 DOC/XLS、压缩包、含宏 Office 文件、加密或带密码文件不支持。
- 前后端均校验大小，但以后端的扩展名、MIME、文件头、加密状态和安全扫描结果为准。
- 文件上传 OBS 后由持久任务执行文本解析、Excel 结构化解析、版面识别或 OCR。
- OCR 结果至少包含文本、页码、区域坐标和置信度；低置信度字段必须标记待确认，不能自动补全。
- 文件只有在 `READY` 状态下才能参与问答；解析中、失败、删除、过期或隔离文件不得被静默忽略。
- 问题受理时通过 `qa_question_file` 固化文件 ID 和本次用途；会话历史按问题恢复附件卡片。逻辑删除不删除既有问题关联，保证对话上下文和审计记录完整。
- Excel 作为 `QUERY_INPUT` 时，应按批准模板或受控表头提取产品编号等参数，执行格式校验、去空、去重、数量上限和逐项权限校验；无效行必须返回行号和原因。
- 例如用户上传包含一批产品编号的 Excel 并询问相关信息时，系统先把产品编号集合并入结构化查询参数，再批量查询 GoldenDB 和/或公司知识库；Excel 本身不作为回答证据，答案引用应指向实际返回事实的 GoldenDB 或公司知识库。
- 批量查询必须有单次记录上限、并发上限、超时和部分成功语义；逐项展示成功、无权限、未找到和失败状态，不能因个别编号失败而静默丢弃整批结果。
- 文件作为 `EVIDENCE` 时，回答引用必须区分“公司知识库”和“用户临时文件”，并展示文件名、工作表/行号、页码或章节等可复核位置。
- 默认由本系统解析和检索相关片段；确需 HiAgent 读取原始视觉内容时，可以生成精确指向单一对象的短时效、只读 HTTPS 签名 URL。
- 签名 URL 不得返回前端、写入回答或完整记录到日志中。

OCR 服务或引擎、病毒扫描服务、Excel 模板/表头、批量记录上限、文件数量、会话总容量和各类文件保留期限为 `TBD`。

### 3.6 范围外

- 合同智能审核、合同差异比对和申赎确认单处理的业务实现。
- 企业知识库文件上传、维护、切片、向量化和索引管理。
- 合规规则编辑、案例库管理和完整用户管理后台。
- Word 原生批注、Word 原生修订稿和非 DOCX 审核报告。
- 让大模型生成任意 SQL、表名、字段名或权限条件并直接执行。
- 将临时文件自动沉淀为公司正式知识。

## 4. 用户、认证与授权

### 4.1 认证模式

系统内部使用统一用户身份模型，业务层不依赖具体 SSO 协议：

```text
corporate：公司统一认证
mock：开发环境模拟认证
```

- `dev` 允许 `mock` 或 `corporate`，默认可使用 `mock`。
- `test` 和 `prod` 只允许 `corporate`。
- `corporate` 模式缺少必要配置时应用必须拒绝启动。
- 不得从公司认证失败自动降级为模拟用户。
- 模拟用户由服务端白名单配置，不能信任浏览器传入任意用户编号。

### 4.2 内部用户信息

统一身份至少包含：

```text
userId、displayName、departmentId、departmentName、roles、permissions、
knowledgeScopes、dataScopes、enabled、authenticatedAt、sessionExpiresAt
```

一期最小权限包括 `QA_ACCESS`、`QA_FILE_UPLOAD`、`QA_HISTORY_READ` 和 `QA_FEEDBACK`。

### 4.3 授权不变量

- 后端只从认证上下文取得用户身份，请求体中的用户编号不可信。
- 会话、回答、反馈、文件、文件片段、SSE 和审计记录均按 Owner 隔离。
- 数据库权限条件必须进入 SQL，不能查询全部数据后在 Java 中过滤。
- 知识库查询必须携带服务端计算的知识权限范围。
- 跨用户访问统一按资源不存在处理，避免泄露资源是否存在。
- HiAgent `UserID` 长度为 1～20；若公司用户标识不满足要求，必须使用稳定、不可逆、无碰撞且可审计的映射。

公司统一认证协议、声明字段和权限来源为 `TBD`。

## 5. 目标 API

以下为第一阶段目标契约；当前已实现接口与目标差异必须在迭代中逐步消除。完整字段应在 `docs/api/intelligent-qa-api.md` 中维护。

### 5.1 用户

```text
GET  /api/v1/me
```

系统不提供 `/api/v1/agents`。前端维护 Agent 目录，问题请求必须携带 `agentType`；后端使用独立枚举白名单识别并路由，第一阶段只受理 `SMART_DATA`，其他已知类型或未知类型均返回 `400`。

### 5.2 会话

```text
POST   /api/v1/chats
GET    /api/v1/chats?cursor=...&limit=...
GET    /api/v1/chats/{chatId}
PATCH  /api/v1/chats/{chatId}
DELETE /api/v1/chats/{chatId}
GET    /api/v1/chats/{chatId}/messages?cursor=...&limit=...
```

列表采用稳定游标。新建会话允许标题为空，服务端先使用默认名称并在首次提问后自动命名。

删除活动会话返回：

```text
HTTP 409
title: Conversation is active
detail: 会话正在执行，请停止后删除
```

此状态冲突不得被前端改写为通用删除失败，也不得自动发起停止；停止必须由用户明确操作。

### 5.3 临时文件

```text
POST   /api/v1/chats/{chatId}/files
GET    /api/v1/chats/{chatId}/files
GET    /api/v1/chats/{chatId}/files/{fileId}
GET    /api/v1/chats/{chatId}/files/{fileId}/content
DELETE /api/v1/chats/{chatId}/files/{fileId}
```

上传接口使用 `multipart/form-data` 和 `Idempotency-Key`。文件内容访问必须再次校验 Owner 和会话归属。

### 5.4 问题与回答

```text
POST /api/v1/chats/{chatId}/questions
GET  /api/v1/answers/{answerId}
GET  /api/v1/answers/{answerId}/events
POST /api/v1/answers/{answerId}/cancellation
POST /api/v1/answers/{answerId}/regenerations
PUT  /api/v1/answers/{answerId}/feedback
```

问题请求包含必填的 `agentType`、`question` 和可选文件引用；每个文件引用至少包含 `fileId` 和 `usage=AUTO|QUERY_INPUT|EVIDENCE`。问题、停止、重生成及其他创建任务的操作使用独立幂等键。

### 5.5 API 通用规则

- JSON 使用 camelCase，时间使用 ISO 8601，持久化使用 UTC。
- 创建会话和文件返回 `201`；问答、停止和异步任务受理返回 `202`。
- 错误响应使用兼容 RFC 7807 的结构，至少包含稳定 `code`、`traceId` 和安全的中文说明。
- `401` 表示未认证，`403` 表示没有功能权限，`404` 表示资源不存在或不可访问，`409` 表示幂等/状态冲突或文件未就绪，`413` 表示文件过大，`415` 表示文件类型不支持，`429` 表示限流。
- 幂等身份至少包含 Owner、操作类型、幂等键和请求指纹；相同键不同请求返回 `409`。
- SSE 事件数据必须结构化，支持阶段、文本增量、结构化引用、证据冲突、人工复核和终态。
- SSE 必须提供可恢复的白盒阶段事件：流程场景与版本、意图类型/置信度/查询条件数量、知识片段数量、GoldenDB 事实数量、证据核验状态/冲突数量、生成完成与最终持久化完成；事件不得携带提示词、思维链、原始 SQL、内部地址、密钥或证据正文。
- 会话列表响应固定为 `items`、`nextCursor`、`hasMore`；客户端不得自行解析游标内容或使用偏移量拼页。
- 历史助手消息返回 `executionEvents` 和 `artifacts`，用于恢复执行过程、来源与产物；敏感内部载荷不得放入这两个展示字段。

## 6. 完整问答处理规则

1. 认证用户，校验请求携带的 `agentType`、对应功能权限、当前开放状态和会话归属；第一阶段只将 `SMART_DATA` 路由到 `DUAL_CHANNEL_QA` 版本化执行计划。
2. 校验问题、幂等键和全部文件引用；文件必须属于当前用户、当前会话且状态为 `READY`，并解析或确认每个文件的业务角色。
3. 在同一 GoldenDB 事务中保存用户问题、回答占位、助手消息占位、问题文件关系、幂等记录和持久生成任务。
4. 工作节点以租约领取生成任务，加载最近有效历史和持久化结构化上下文。
5. 先将 `QUERY_INPUT` 文件中的受控结构化数据合并为问题参数，再结合原问题执行意图识别、实体提取和指代消解；每个参数保留文件、工作表、行号和原始值来源。
6. 低置信度、缺少参数或多候选时保存 `NEEDS_CLARIFICATION` 追问；此分支不得查询知识库、业务数据库、文件证据或调用模型。
7. 参数完整后，业务数据库通道把意图和实体转换为标准业务语义，经过语义字段白名单、受控 Query Plan、固定语句编译后查询 GoldenDB；同时查询公司知识库，只有标记为 `EVIDENCE` 的文件内容才作为第三类证据参与检索。
8. 对产品、日期、费率、金额、人员、文档版本和 OCR 结果进行确定性标准化。
9. 将证据评估为 `CONSISTENT`、`CONFLICT`、`INSUFFICIENT`、`SINGLE_SOURCE`、`DEPENDENCY_FAILED` 或 `LOW_CONFIDENCE`。
10. 证据不足时返回确定性拒答；必要依赖失败不得伪装成普通空结果。
11. 冲突时先持久化并输出各来源值、来源时间、冲突字段和人工复核提示，再允许模型整理语言。
12. 构造只包含受控规则、原问题、已确认实体、历史摘要、证据和输出约束的 HiAgent `Query`。
13. 每次生成创建独立 HiAgent 会话；重新生成把原问题及其附件引用复制为新的完整问答轮次，重新查询证据并创建新回答，不调用 `/query_again` 复用旧证据。
14. 需要原始视觉内容时，为当前任务生成短时效 OBS URL 并通过 `QueryExtends.Files` 传给 HiAgent，`PubAgentJump` 固定为 `false`。
15. 每个准备发送给客户端的文本增量必须先条件持久化到 GoldenDB，再发布 Redis 事件和 SSE。
16. 完成时将最终回答、助手消息、证据快照、引用、会话上下文和生成任务状态放入同一事务或可靠事件闭环。
17. 浏览器断线不取消生成；使用 `Last-Event-ID` 从 Redis Stream 重放，事件缺口时读取 GoldenDB 快照。

双通道问答采用场景计划驱动的节点化编排。场景计划集中声明问题理解、知识库检索、GoldenDB 查询、证据对账和回答生成节点；节点不得自行枚举和判断全部场景，只能判断自身数据前置条件。执行器负责节点间停止检查、前置条件跳过、合法短路和执行记录，低置信度或歧义在问题理解节点后立即短路，严禁进入下游证据和模型节点。

建议同一会话同时只允许一个活动回答；不同会话可以并发。最终并发规则需产品确认。

## 7. 文件处理规则

### 7.1 文件状态

```text
UPLOADING -> STORED -> SECURITY_CHECKING -> PARSING/OCR -> READY
REJECTED / PARSE_FAILED / QUARANTINED / DELETE_PENDING / DELETED / EXPIRED
```

### 7.2 文件处理

- 原始文件名只用于安全显示，对象 Key 由服务端生成，不包含用户姓名或未处理文件名。
- OBS Bucket 保持私有，凭据由密钥系统提供，启用 TLS、加密和生命周期策略。
- 文本、Excel 或 OCR 解析使用持久任务、租约、有界重试、死信和重启恢复，不能只投递本机线程池。
- Excel 解析保留工作表、表头、行号、单元格原始值和规范化值；公式默认不执行，只读取可信缓存值或按安全规则拒绝。
- 查询输入数据与证据片段使用不同的数据类型和存储字段，禁止把产品编号列表等查询条件自动加入引用或证据对账。
- OCR 至少执行方向校正、版面识别、文本和坐标提取、置信度判断；表格能力取决于最终 OCR 服务契约。
- 文件片段包含文件、页码、章节、内容、坐标、偏移、OCR 置信度和解析器版本。
- 文件删除先使其不可用于新问题，再通过持久任务删除 OBS 原始对象、派生文本和片段。
- 数据库与 OBS 不能形成单一事务，必须使用补偿和孤儿对象对账。

## 8. 三来源证据规则

### 8.1 来源定位

- GoldenDB：产品、交易和其他结构化业务事实的权威来源。
- 公司知识库：公司正式产品文档的权威来源。
- 用户临时文件：仅当角色为 `EVIDENCE` 时才是用户提供的待验证材料，且不自动视为公司正式依据。
- 查询输入文件：只负责提供实体、筛选条件或批量参数，不是证据来源，不参与来源冲突，也不能被回答引用为事实依据。

### 8.2 标准化和冲突

- `0.0030`、`0.30%` 和“百分之零点三”等价值标准化后应判断一致。
- “最新”必须同时考虑产品、文档类型、有效状态、生效/失效日期、版本号和发布日期。
- `EVIDENCE` 临时文件与正式来源冲突时不能覆盖 GoldenDB 或公司知识库。
- 高风险字段包括费率、日期、金额、产品状态、人员和文档版本；这些字段的冲突不得漏报。
- 引用由系统根据证据项生成，不能完全依赖模型自由输出。

单通道回答规则需按意图配置：产品/交易结构化事实优先依据 GoldenDB，正式文档内容优先依据公司知识库。依赖调用失败与成功空结果必须严格区分。

## 9. 回答、停止与反馈状态

回答状态：

```text
PENDING -> UNDERSTANDING -> RETRIEVING -> RECONCILING -> GENERATING -> COMPLETED
                     \-> NEEDS_CLARIFICATION
任意非终态 -> CANCEL_REQUESTED -> CANCELLED / CANCEL_FAILED
生成前异常 -> FAILED
已产生部分内容后异常 -> INCOMPLETE
```

- `COMPLETED` 保存完整正常回答。
- `NEEDS_CLARIFICATION` 保存追问并持久化待补上下文。
- `CANCELLED` 保存停止前的部分内容。
- `CANCEL_FAILED` 显示远端停止失败，不能伪装为已停止。
- `INCOMPLETE` 保存异常前已经生成的部分内容。
- 完成、失败、文本增量和停止均使用条件更新，保证并发时只有一个合法终态。
- HiAgent 1.5.0 及以后使用 `MessageID` 调用 `/stop_message`；无法取得消息 ID 时必须失败关闭。
- 反馈先保存本地 GoldenDB，再通过持久任务异步同步 HiAgent：`LIKE=1`、`DISLIKE=-1`、`NONE=0`。远端失败不回滚本地反馈。

## 10. 数据与一致性

- GoldenDB 是会话、问题、回答、状态、结构化上下文、文件元数据、任务、证据、引用、反馈和审计记录的事实来源。
- Redis 用于跨实例事件发布、有限重放、协调和限流，不得成为回答或任务的唯一事实来源。
- 应用运行数据和公司业务数据使用独立数据源配置及最小权限账号；业务数据源只读。
- Flyway 管理本系统 `qa_*` 表及 `dws_product_info_d` 空表结构；数据中台同步任务负责产品快照数据，不由 Flyway 写入业务数据。
- 已执行的 `V1`～`V4` 不得修改，后续使用 `V5` 及更高版本前向迁移。
- 目标新增模型包括通用幂等记录、持久生成任务、用户映射、临时文件、文件解析产物、结构化查询输入、文件片段、文件任务、带使用角色的问题文件关系、证据快照、证据项、结构化引用、HiAgent 反馈任务和审计事件。
- 所有 Owner 查询、逻辑删除、任务领取和列表查询必须有适当复合索引和明确上限。
- 完整模型与事务边界以 `ARCHITECTURE.md` 为准。

## 11. 外部系统边界

### 11.1 公司统一认证

协议类型、三套环境、用户声明、会话有效期和网关信任机制为 `TBD`。浏览器优先使用安全 HttpOnly Cookie，不在本地存储 Token。

### 11.2 公司知识库

查询契约至少表达问题、已确认实体、用户知识权限、文档类型、有效时间和结果上限；响应至少包含稳定片段/文档 ID、产品编码、文档类型、版本、发布日期、生效日期、页码、内容和受权原文入口。具体 API、认证、错误码、限流和 SLA 为 `TBD`。

### 11.3 GoldenDB

已确认产品类 SQL 直接查询数据中台同步到 GoldenDB 的 `dws_product_info_d`。查询先以 `MAX(DT) <= 业务日期` 确定最新有效快照，再按产品代码、名称、简称或全称解析唯一产品；投资经理同时返回监管和产品部口径；“今日基准日产品”同时查询定开基准日和产品到期日。GoldenDB 版本、拓扑、产品数据权限策略、只读账号、独立数据源、连接预算、`EXPLAIN` 证据和三套环境仍为 `TBD`，因此生产开关默认关闭。

### 11.4 HiAgent

使用 `/create_conversation`、`/chat_query`、`/stop_message` 和建议接入的 `/feedback`。`UserID` 长度 1～20，`ResponseMode=streaming`，`PubAgentJump=false`。真实 SSE 增量/累计语义、`MessageID`/`TaskID`、结束/错误/心跳事件、认证、必填 `Inputs`、并发和数据留存为 `TBD`。

### 11.5 OBS 与 OCR

OBS 厂商/兼容性、Endpoint、Bucket、IAM、KMS、签名 URL、网络和生命周期为 `TBD`。OCR 引擎/API、坐标与置信度格式、表格能力、限流、数据流向和准确率基线为 `TBD`。Excel 模板、表头别名、必填列、单批记录上限、公式处理和错误行反馈契约为 `TBD`。

## 12. 非功能需求

### 12.1 性能和容量

| 指标 | 第一阶段目标 |
|---|---:|
| 问答提交量 | 持续 10 QPS |
| 并发用户 | 不少于 50 |
| 问题受理接口 | 建议 P95 不超过 1 秒，待签署 |
| 首个任务状态事件 | 建议 P95 不超过 2 秒，待签署 |
| 简单问答完整回答 | 不超过 10 秒；统计分位为 `TBD`，建议 P95 |
| 复杂问答完整回答 | 不超过 20 秒；统计分位为 `TBD`，建议 P95 |
| 1 MiB 以内短文件解析/OCR | 建议 P95 不超过 30 秒，待签署 |
| 容量余量 | 峰值验证基础上不少于 30% |
| 持续压测 | 使用真实依赖不少于 30 分钟 |

所需并发生成数至少按 `10 × P95 完整回答秒数` 计算，不能把本地线程池默认值视为生产容量。

### 12.2 可用性

- 数据接口目标可用性不低于 99.5%。
- AI 服务目标可用性不低于 99.0%，受 HiAgent SLA 约束。
- 本系统 SLO、错误预算、RPO、RTO、维护窗口和降级策略为 `TBD`。
- 生产至少两个副本跨故障域部署；网关关闭 SSE 缓冲并配置合适空闲超时。

### 12.3 安全与合规

- 禁止在日志中记录完整问题、答案、Query、知识片段、数据库结果、文件内容、签名 URL、平台会话 ID或凭据。
- 所有远程调用设置超时、并发上限、有界重试、退避和可观测错误分类。
- 必须测试统一认证伪造、跨用户访问、SQL 注入、提示词注入、知识库越权、数据库越权、恶意文件、OBS URL 泄露和模型数据外发。
- 文件执行扩展名/MIME/魔数、加密、宏、嵌入对象、压缩炸弹、恶意软件和解析资源检查。
- 数据分类、保留/删除、法律保全、模型训练/留存、内容安全和投诉机制为 `TBD`。
- 生产发布前完成 SAST、依赖、许可证、密钥、镜像和 SBOM 检查。

## 13. 准确性和验收指标

以下为建议发布门禁，最终由业务负责人批准：

| 指标 | 建议最低值 |
|---|---:|
| 意图分类准确率 | 95% |
| 核心实体抽取 F1 | 95% |
| 交易流水号完全匹配准确率 | 99% |
| 指代消解准确率 | 95% |
| 无法证明却自动选择实体 | 0 |
| 澄清正确率 | 95% |
| 知识检索 Recall@8 | 90% |
| 最新有效文档选择准确率 | 100% |
| 一般冲突检出召回率 | 95% |
| 高风险字段冲突检出召回率 | 100% |
| 引用正确率 | 100% |
| 事实有据率 | 100% |
| 发布黄金集无依据内部事实 | 0 |
| OCR 字符准确率 | 95% |
| OCR 关键字段准确率 | 95% |
| 端到端问答准确率 | 最低 90%，目标 95% |

“零幻觉”的可执行定义是：发布黄金集中不允许出现没有证据来源支撑的内部业务事实。系统不能对全部未知输入承诺绝对无错误，但证据不足、冲突或低置信度时必须拒绝猜测或提示人工复核。

黄金集建议不少于 300 个经业务签署的样本，覆盖产品基础信息、费率、最新文档、交易员、多轮上下文、歧义、无证据、三来源冲突、权限、安全攻击及清晰/模糊/倾斜/表格 OCR。

## 14. 功能验收重点

- 公司认证、开发模拟认证及生产禁用保护。
- 前端本地 Agent 展示、`SMART_DATA` 提交、后端类型识别和未开放提示。
- 会话自动命名、分组、游标分页及跨会话隔离。
- 合法文件、Excel 批量查询输入、扫描 PDF、图片 OCR、低置信度、恶意文件及跨用户文件访问。
- 查询输入文件不得被误标为证据；证据文件不得绕过标准化、引用和冲突检测。
- 指代消解、多候选、缺参数、追问恢复、主题切换和跨重启上下文。
- GoldenDB 受控查询、权限 SQL、空结果、超时和真实兼容性。
- 知识库权限、多版本、旧版高相关度反例、空结果、限流和提示词注入。
- 两类正式来源与可选文件证据的一致、单来源、依赖失败、OCR 低置信度和高风险字段冲突。
- HiAgent 创建会话、真实 SSE、空流、畸形事件、文件 URL、停止和反馈。
- 增量先持久化、Redis 多副本重放、事件缺口回退、生成任务重启恢复。
- 正常回答、追问、停止、异常部分内容、重新生成和反馈全部持久化。
- 10 QPS、50 并发、30% 余量、安全、故障注入、备份恢复及发布回滚。

## 15. 开发迭代顺序

1. 冻结外部契约和字段字典。
2. 使用 V5+ 前向迁移升级 API 与数据模型。
3. 接入公司统一认证及权限校验；前端本地 Agent 入口、后端类型白名单、开发模拟认证及非开发环境启动保护已完成。
4. 以持久生成任务和 Redis Stream 替换本机线程投递及内存事件流。
5. 将现有一期业务语义查询骨架接入真实 GoldenDB 授权视图，并完成生产意图识别。
6. 接入公司知识库。
7. 实现 OBS、文件解析、OCR、文件检索和清理。
8. 实现查询输入与证据隔离，以及可选三来源证据标准化与冲突检测。
9. 完成 HiAgent 正式认证、真实 SSE、文件 URL、停止和反馈联调。
10. 完成前端整合、准确性、安全、容量、故障恢复和生产验收。

每个迭代都必须同步中文注释、测试、API/架构文档和生产就绪清单，并通过后端 `mvn -B -ntp clean verify` 与前端 `npm run verify`。

## 16. 当前实现基线与差距

当前已具备：前端本地 Agent 目录与未开放提示、提问 `agentType` 参数和后端类型路由、只注册 `DUAL_CHANNEL_QA` 的版本化场景计划、可复用节点与轻量执行器、会话 CRUD、首次提问落库、稳定游标分页、前端虚拟滚动和今天/昨天/更早分组、活动会话删除保护、可恢复用户消息附件的历史消息、问题提交、回答快照、GoldenDB 执行事件与来源/产物恢复、SSE、停止、重生成、反馈、持久追问/上下文、双通道编排骨架、一期产品/交易业务语义目录、受控 Query Plan、固定语句 SQL Compiler、带 Owner 条件的 MyBatis 查询、临时文件上传/列表/删除 API、V5 文件元数据与问题引用、V6 回答事件历史、开发本地对象存储适配器、HiAgent 创建会话/流式/停止客户端、MyBatis-Plus 系统数据持久化、Jetty、XML 环境配置、开发模拟认证和非开发环境启动保护，以及本地质量门禁。

当前未达到第一阶段目标的主要部分：

- 公司统一认证生产适配器、身份声明映射及权限传递；`corporate/mock` 运行开关与开发模拟用户已经实现。
- 公司权限驱动的后端 Agent 功能授权校验；目录展示继续由前端配置，不能替代后端鉴权。
- 服务端会话自动命名策略；当前由前端在首次有效提问时生成安全截断标题并随会话一起落库。
- 真实 OBS 适配器、恶意文件检测、持久文件任务、Excel/文本解析、扫描 PDF/图片 OCR、文件检索、短期签名 URL 和生命周期清理；当前开发环境只完成受限上传联调。
- 公司知识库正式适配器。
- GoldenDB 真实授权视图、字段口径、独立只读数据源、发布前 `EXPLAIN` 证据和生产意图识别器。
- 查询输入与证据隔离、可选三来源结构化证据和生产级对账。
- HiAgent 正式认证、真实 SSE 事件契约、文件 URL 和反馈同步。
- Redis Stream、持久生成任务、多实例恢复和通用幂等指纹。
- 完整审计、安全、准确性、10 QPS、50 并发和真实环境验证。

因此当前工程仍是可扩展的生产工程骨架，不能描述为可直接上线的第一阶段成品。

## 17. 发布阻塞项

下列任一项未完成都阻塞生产发布：

- 公司统一认证和权限传递未通过；测试与生产虽已禁止模拟认证、生产已禁止演示回答，但正式认证适配器仍未完成。
- GoldenDB 业务字段、权限视图、只读账号和真实兼容性未确认。
- 公司知识库权限、版本、来源、限流和真实环境契约未通过。
- OCR 服务、文件安全、OBS IAM/加密/签名 URL/生命周期未通过。
- HiAgent 认证、必填 Inputs、真实 SSE、MessageID、结束/错误事件和容量未通过。
- Redis Stream 或等价跨实例事件能力、持久生成任务和故障恢复未完成。
- 回答终态、证据、引用和结构化上下文没有可靠事务/补偿闭环。
- 业务黄金集存在无依据事实或高风险冲突漏报。
- 10 QPS、50 并发及至少 30% 余量未通过真实依赖压测。
- 数据分级、保留删除、安全、运维、监控、备份、发布和回滚未完成签署。

生产就绪证据统一维护在 `docs/operations/production-readiness-checklist.md`。
