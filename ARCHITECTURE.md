# AI 赋能项目第一阶段架构设计

> 本文描述第一阶段“智能问答”的目标架构，并明确当前代码基线与待建设能力。统一工作台交互以飞书《产品需求文档》和后续确认结论为输入；后续确认优先于文档中的冲突表述。文中标记为“目标”的内容不代表当前已经实现。

## 1. 架构状态与已确认决策

| 主题 | 已确认结论 |
|---|---|
| 一期入口 | Agent 目录由前端本地配置；选择器只在新建会话首次提问前展示，首问携带 `agentType=SMART_DATA` 并固化到会话，后续提问不再传递且不可修改 |
| 身份认证 | 未认证用户先跳转公司统一认证页面；一期不建设自有登录页。正式环境接入公司统一认证，开发环境可通过开关使用模拟用户 |
| 会话创建 | 新聊天保持前端空白草稿，第一次有效提问时才创建并落库 |
| 删除保护 | 活动回答存在时后端以 `409` 拒绝删除，固定提示“会话正在执行，请停止后删除” |
| 知识库 | 公司知识库平台负责上传、维护、解析、切片和索引；本系统仅通过接口检索知识片段 |
| 业务数据 | 数据中台将数据同步到 GoldenDB，本系统通过原生 MyBatis 直接执行受控只读查询 |
| 临时文件 | 2026-09-14 调整：一期不支持文件上传；相关接口、页面入口、OBS 和 OCR 均不启用 |
| 证据策略 | 一期只标准化 GoldenDB 与公司知识库证据；冲突时并列展示并提示人工复核 |
| 容量目标 | 问答提交量 10 QPS，同时在线并发用户 50 人 |
| 运行时 | Java 8、Spring Boot 2.7、Maven、内嵌 Jetty、XML Properties 环境配置 |

仍待确认的外部条件包括：公司统一认证协议、知识库检索契约、HiAgent 真实流事件、GoldenDB 拓扑与方言、Redis 集群和生产安全合规要求。上述事项均属于生产发布阻塞项，不得由代码自行假设。OBS、OCR 和文件生命周期不再阻塞一期发布，统一顺延到后续阶段。

## 2. 系统上下文

```text
公司内部用户
      │
      ▼
公司统一认证 / 开发模拟用户
      │
      ▼
前端 Web ── REST + SSE ──> 智能问答服务
                              │
                 意图识别、实体提取、指代消解、追问
                              │
                      ┌───────┴───────┐
                      ▼               ▼
                公司知识库平台   GoldenDB 业务数据
                    检索接口       受控只读查询
                      └───────┬───────┘
                              ▼
               证据标准化、去重、版本判断与冲突检测
                              │
                              ▼
                       公司 HiAgent API
                              │
                    流式答案、引用与风险提示
                              │
             GoldenDB 持久状态 + Redis Stream 事件
```

GoldenDB 是业务状态和最终回答的事实来源；Redis 只承载可重建的流事件、短期协调和缓存，不得成为唯一事实来源。已有文件元数据表仅用于兼容开发期历史数据和后续扩展，一期不产生新的文件数据。

## 3. 架构原则与六边形边界

```text
com.acme.intelligentqa
├── domain
│   ├── model
│   └── port
│       ├── in                    # 会话、文件、问答、停止、反馈用例
│       └── out                   # 认证、GoldenDB、知识库、OBS、OCR、模型、事件流
├── application
│   ├── service                   # 用例、状态机、幂等、事务与失败语义
│   └── workflow                  # 场景计划、执行上下文、通用执行器与可复用节点
├── adapter
│   ├── in.web                    # REST、SSE、统一异常处理
│   └── out
│       ├── authentication        # 公司统一认证与开发模拟用户
│       ├── persistence           # 原生 MyBatis 与迁移脚本
│       ├── knowledge             # 公司知识库平台
│       ├── storage               # OBS
│       ├── ocr                   # OCR 服务
│       ├── ai                    # 公司 HiAgent
│       └── stream                # Redis Stream / 本地开发实现
├── config
└── common.error
```

依赖只能指向内层。领域层不得依赖 Spring、MyBatis、JDBC、Redis、OBS SDK、OCR SDK 或模型厂商 SDK。外部系统均通过端口接入，便于在外部协议确认后替换适配器而不改动核心用例。ArchUnit 负责强制执行关键依赖规则。

禁止让大模型生成并执行任意 SQL；禁止把知识库检索、OCR 或模型调用放入数据库事务；禁止用 Redis 或浏览器状态替代 GoldenDB 中的业务事实。

## 4. 运行时与环境配置

服务使用 Spring Boot 2.7.18 可执行 JAR 和内嵌 Jetty，通过 `java -jar` 启动，不使用 Tomcat，也不依赖外部应用服务器。Maven 必须排除 `spring-boot-starter-tomcat` 及全部 `org.apache.tomcat.embed` 组件。

配置采用 Java 标准 XML Properties 格式：

- `application.xml`：跨环境且不含秘密的公共配置。
- `application-dev.xml`：开发环境，可按开关启用模拟用户和本地适配器。
- `application-test.xml`：测试环境，使用隔离的测试依赖，禁止模拟用户绕过测试认证契约。
- `application-prod.xml`：生产环境，强制统一认证和真实外部适配器。

部署通过 `SPRING_PROFILES_ACTIVE` 选择且只能选择 `dev`、`test`、`prod` 之一。`app.runtime.stage` 必须与 Profile 一致。密钥、密码、令牌和真实地址只能通过环境变量或密钥平台注入，不进入仓库。

认证模式建议使用 `app.auth.mode=mock|corporate`，并由启动校验器强制：

- `dev` 可配置为 `mock` 或 `corporate`；模拟用户必须有醒目标识和固定审计字段。
- `test`、`prod` 只能使用 `corporate`；发现 `mock` 时启动失败。
- 业务代码只依赖 `CurrentUserPort`，不得读取前端传入的用户 ID 作为可信身份。

Jetty 线程、连接空闲超时、SSE 长连接、上传大小、请求队列和优雅停机参数必须通过容量测试确定。

## 5. 接口入口与权限边界

目标接口按职责分组：

- 当前用户：获取已认证用户信息和当前认证模式。
- Agent：前端负责目录展示；后端不提供列表接口。首问按 `agentType` 执行白名单、开放状态、权限校验和处理链路路由，并持久化到 `qa_conversation.agent_type`；后续提问从已锁定会话读取类型，不接收类型切换。
- 会话：首次提问时随问题原子创建、分页查询、重命名和逻辑删除。
- 文件：上传、查询处理状态、删除未引用文件。
- 问答：提交问题、查询回答快照、订阅 SSE、停止、重新生成、反馈。

所有接口从认证上下文取得 `ownerId`，并按用户和会话双重隔离。任何会话、回答、文件、证据、反馈和上下文查询都必须带 Owner 条件；资源不存在与无权访问应使用不泄露资源存在性的统一语义。

列表接口使用由 `updatedAt + id` 组成的不透明稳定游标，而不是无界偏移。前端按需取下一页，并使用虚拟滚动限制 DOM 节点数量。写接口使用幂等键和请求指纹；同一用户、同一操作、同一指纹可复用结果，同一幂等键对应不同请求必须返回冲突。

面向前端的 Spring MVC 业务接口仅允许 GET 和 POST。GET 保持安全、只读且可缓存语义；提问、重命名、删除、反馈、停止和重生成等状态变更统一使用 POST。每项业务操作使用全局唯一 URL，例如统一提问使用 `/questions/submission`，重命名与删除使用 `/rename`、`/deletion`；禁止依靠 GET/POST 或请求体差异区分相同路径，也禁止注册 PUT、PATCH、DELETE 或未限定 HTTP 方法的前端映射。运行时映射测试扫描实际控制器注册表，同时校验方法白名单与 URL 唯一性。后续阶段若重新启用文件能力，其写操作同样遵循 POST 与唯一动作路径约束。

统一提问由应用服务事务协调：`chatId` 为空时，以认证用户、幂等键创建或复用会话，再创建问题和回答；`chatId` 非空时锁定并校验已有会话，沿用同一问答流程。事务提交后才投递异步编排任务，避免消费者读取未提交数据。`qa_conversation.owner_id + creation_idempotency_key` 唯一约束与回答请求指纹共同防止网络重试或并发请求产生重复会话、问题和回答。

删除会话前在应用层检查是否存在活动回答。活动状态集合为 `PENDING`、`RETRIEVING`、`QUERYING`、`GENERATING` 和 `CANCEL_REQUESTED`；命中时拒绝逻辑删除并返回固定的 `409` 业务错误。前端只展示提示，不得擅自替用户停止任务。停止到达明确终态后，用户可以再次删除。

## 6. GoldenDB 与持久化设计

### 6.1 两类 GoldenDB 数据

1. 应用业务库：保存会话、消息、回答、文件元数据、证据快照、任务和审计事件。
2. 数据中台同步业务库：保存产品、交易、公告等业务事实，由本系统执行受控只读查询。

两类数据即使部署在同一 GoldenDB 集群，也必须使用独立的数据源配置、账号权限和访问适配器。业务查询账号只读，禁止 DDL/DML；查询必须经过审核目录，限制超时、最大行数、最大字节数并记录审计信息。

### 6.2 原生 MyBatis 约束

- 使用 `mybatis-spring-boot-starter` 2.3.2，并显式固定 MyBatis 3.5.19，兼容 Java 8 与 Spring Boot 2.7。
- Mapper 只声明仓储实际需要的具名方法，不继承通用 CRUD 接口；持久化 Record 不使用 ORM 注解。
- 插入、条件更新、Owner 隔离、逻辑删除、联表、幂等、游标分页和有界排序查询全部使用固定参数化 SQL 与显式 ResultMap。
- 禁止 `SELECT *`、无界列表、动态拼接表名/列名和模型生成 SQL。
- 所有数据表仅使用 `id BIGINT AUTO_INCREMENT` 物理主键，禁止 `pk_id`、UUID 和复合业务主键；数据库生成值只留在持久化适配器。
- 对外资源继续使用不可变 UUID，并在资源表映射为 `public_id UNIQUE`；Owner、游标、上下文和 OBS 不得使用可枚举的数据库自增值。
- 持久化 Record 的 Java 字段名必须由数据库列名直接转换为小驼峰：去掉下划线，并将下划线后的首字母大写，例如 `app_conversation_id` 对应 `appConversationId`、`message_id` 对应 `messageId`、`public_id` 对应 `publicId`。禁止使用 `databaseId`、`providerMessageId` 等偏离物理列语义的别名；领域模型和 API DTO 可以按业务语义命名，但必须在适配器中显式转换。
- Spring 数据访问异常转换为稳定的领域/应用异常。

### 6.3 一期业务语义查询引擎

现有问答编排继续依赖 `BusinessDataQueryPort`，端口内部新增以下确定性流水线：

```text
已消歧 QueryIntent
        │
        ▼
PhaseOneBusinessSemanticParser
  只产生产品/交易实体和标准业务字段
        │
        ▼
PhaseOneBusinessSemanticCatalog
  校验实体字段白名单和一期关系约束
        │
        ▼
PhaseOneBusinessQueryPlanner
  PRODUCT_LOOKUP / PRODUCT_REFERENCE_DATE_LIST / TRADE_LOOKUP
        │
        ▼
PhaseOneBusinessSqlCompiler
  SELECT_PRODUCT_FACTS / SELECT_TRADE_FACTS
        │
        ▼
BusinessQueryMapper 固定参数化 SQL
        │
        ▼
dws_product_info_d / 兼容既有交易语义视图
```

产品主题元数据来自已发布的语义层配置：数据集注册描述可查询数据集及数据范围，字段注册描述字段含义、筛选/分组能力和同义词，枚举注册描述允许用于筛选的标准枚举值。模型只输出标准意图、业务字段和结构化筛选条件，不输出可执行 SQL；系统通过固定目录映射到物理字段。

当前只有产品主题表 `dws_product_info_d`，因此数据集选择和关系路径规划均退化为确定性单表选择，不执行 Join。产品 Query Planner 支持单产品字段查询和业务日期产品列表查询，SQL Compiler 只选择预批准 MyBatis Statement ID，所有用户值通过 `#{}` 参数绑定。

产品物理表契约：

- `dws_product_info_d` 是数据中台同步的产品主题每日全量快照表，物理主键为自增 `id`，业务唯一键为 `PRDC_CD + DT`。
- `PROD_MAT_DT`、`PERI_OPEN_BASE_DT` 和 `DT` 均使用 `VARCHAR(10)`，同步时必须校验为真实有效的 `yyyy-MM-dd` 日期。
- 产品实体按代码、名称、简称和全称检索；多个候选时在知识库和回答模型调用前追问用户。
- 投资经理同时返回 `INVS_MNGR_NM` 监管口径和 `PRDC_INVS_MNGR` 产品部口径。
- 日期列表在最新有效快照中使用 `PERI_OPEN_BASE_DT = 业务日期 OR PROD_MAT_DT = 业务日期`，并分别保留两个命中标志。
- Flyway 创建空表和索引，数据中台同步任务负责业务数据及完整快照的原子发布。
- 产品主题表尚未包含行级用户权限字段；生产启用前必须确认全员可见或补充服务端数据权限方案，不得假定 `ownerId` 已进入 SQL。

### 6.4 场景计划驱动的问答工作流

问答应用层不再把双通道流程固定堆叠在单个服务方法中，而是使用以下边界：

```text
AgentType.SMART_DATA
        │ 后端校验并路由
        ▼
QueryScenario.DUAL_CHANNEL_QA
        │
        ▼
ScenarioPlanRegistry（计划版本 1）
        │
        ▼
QuestionWorkflowEngine
        │
        ├─ QuestionUnderstandingWorkflowNode
        ├─ KnowledgeRetrievalWorkflowNode
        ├─ BusinessDataQueryWorkflowNode
        ├─ EvidenceReconciliationWorkflowNode
        └─ AnswerGenerationWorkflowNode
```

`AgentType` 表示前端业务入口，`QueryScenario` 表示后端证据与生成策略，二者不得混用。第一阶段只存在 `DUAL_CHANNEL_QA`，没有纯 LLM、制度库或其他可执行计划。

场景计划集中定义节点顺序和版本，节点不判断场景枚举。节点只允许通过 `shouldExecute` 判断自身数据前置条件；不满足时由执行器记录 `SKIPPED` 并自动进入下一节点。节点返回 `STOP` 时表示追问等合法短路，后续节点不得执行。执行器在每个节点之间检查停止请求，并在 `QuestionWorkflowContext` 中记录 `STARTED`、`SUCCEEDED`、`SKIPPED` 或 `FAILED`。

`QuestionAnswerService` 继续负责回答状态机、现有 SSE 事件、上下文持久化、文本增量先落库、完成/失败收敛和停止调度。这样节点化改造不改变客户端协议，也不让工作流节点直接控制 HTTP 或持久化适配器。

当前计划为确定性串行计划。知识库和 GoldenDB 并行查询、计划版本持久化以及跨重启完整节点检查点，随持久生成任务和 Redis Stream 后续建设，不在本次重构中虚构完成。

开关 `app.business-query.enabled` 在三套环境分别配置，默认关闭；`maximum-rows` 限制最终计划行数，`semantic-model-version` 进入计划标识和事实追溯信息。未启用时开发演示模式返回空业务结果，非演示环境失败关闭。

当前实现完成了本地 H2 MySQL 模式下的最新快照选择、产品实体解析、两种经理口径查询、基准日/到期日列表查询、固定语句、参数绑定和数量上限验证。真实 GoldenDB 方言、数据权限、独立只读数据源、静态 SQL 审查、`EXPLAIN` 与成本门禁仍是生产启用前置条件。自由 Schema 检索、动态 Join、复杂指标和通用语义查询服务留待后续阶段。

### 6.4 目标数据模型

现有基线表：

- `qa_conversation`：会话归属、名称、创建/更新时间和逻辑删除时间。
- `qa_message`：用户与助手消息，记录消息状态和关联回答。
- `qa_answer`：回答状态、Trace ID、最终内容、错误码、停止和重新生成关系。
- `qa_answer_feedback`：用户对回答的当前喜欢/不喜欢状态。
- `qa_answer_cancel_task`：停止任务、重试、租约和下一次执行时间。
- `qa_conversation_context`：结构化当前实体、来源消息、上一意图、待追问候选和乐观版本。
- `qa_file`：文件名、类型、大小、对象键、归属、哈希、用途和处理状态。
- `qa_question_file`：问题与文件的不可变关联，并保存该次问题使用的 `AUTO`、`QUERY_INPUT` 或 `EVIDENCE` 角色。
- `qa_answer_event`：回答执行阶段、引用/产物、人工复核和流式事件的持久历史；事件 ID 单调递增，用于刷新恢复和断线重放。

后续前向迁移需增加或完善：

- `qa_idempotency_record`：操作类型、幂等键、请求指纹和返回资源。
- `qa_generation_task`：生成任务、租约、重试、心跳、恢复和死信信息。
- `qa_user_identity_mapping`：公司统一认证主体与内部用户标识映射。
- `qa_file_input_item`：从 Excel 等文件提取的受控查询参数、工作表、行号、原始值、规范化值和校验状态。
- `qa_file_chunk`：作为证据使用的解析/OCR 文本分块、页码、工作表/行号、位置和内容摘要。
- `qa_file_task`：上传后解析、OCR、重试和租约任务。
- `qa_evidence_snapshot`：一次回答使用的证据集合、版本和对账结论。
- `qa_evidence_item`：来源类型、来源 ID、字段、值、时间、权限标签和引用信息。
- `qa_citation`：回答片段到证据项的引用关系。
- `qa_feedback_task`：反馈同步到外部平台的重试任务。
- `qa_audit_event`：认证、查询、文件访问、模型调用、停止和管理操作审计。

现有 `V1`～`V5` 迁移不得回改；新增结构使用后续前向迁移。所有 DDL 必须在目标 GoldenDB 验证 SQL 方言、索引长度、时间精度、事务、在线 DDL、执行计划和分布式拓扑影响。

## 7. 后续阶段的临时文件、OBS 与 OCR

本章是后续扩展设计，不属于第一阶段运行拓扑、API 或验收范围。第一阶段前端不展示上传入口，后端通过 `app.file.upload-enabled=false` 不注册文件控制器，并拒绝问题请求携带文件引用。

### 7.1 文件处理流水线

```text
初始化上传
   │
   ▼
校验扩展名/MIME/大小/数量/哈希/权限
   │
   ▼
上传到私有 OBS 对象键
   │
   ▼
UPLOADED -> SCANNING -> PARSING/STRUCTURED_PARSING/OCR_PROCESSING -> READY
                    └──────────────> FAILED
```

后续阶段拟定单文件大小上限为 1 MiB。支持的最终类型包括 XLSX、文本 PDF、扫描 PDF、DOCX、TXT、MD、JPG、JPEG 和 PNG。单问题文件数量、Excel 单批记录上限、用户总配额、OCR 引擎和保留期限仍待确认，必须配置化且默认保守。

文件处理结果分为两类，不得混用：

- `QUERY_INPUT`：提取产品编号、交易流水号、时间范围等查询参数，保留工作表、行号、原始值和规范化值。系统对参数执行格式校验、去重、数量限制和逐项权限校验，然后并入结构化问题。
- `EVIDENCE`：提取可支撑答案的正文或结构化事实，进入证据快照、引用和冲突检测。

`AUTO` 模式可根据文件结构和用户问题识别角色，但识别结果及依据必须持久化；置信度不足或两种用途均可能时先向用户确认。例如包含一批产品编号的 Excel 用于批量查询时，Excel 只是查询输入，最终答案的事实来源仍是 GoldenDB 或公司知识库，不能把该 Excel 列为回答引用。

Excel 解析不得执行公式或宏；默认只读取安全的单元格值。批量查询采用有界分片和并发控制，逐项返回成功、无权限、未找到、参数错误或依赖失败，不得静默遗漏失败行。

OBS Bucket 必须私有。对象键由系统生成，不使用原始文件名；文件名只作为元数据保存并做安全净化。下载 URL 只能由后端按需生成短期、只读、单对象签名地址，不返回给普通前端日志或长期持久化。

HiAgent 调用需要文件时，将短期 OBS URL 放入平台规定的文件参数。签名有效期应覆盖排队、调用和合理重试时间，但不得无限延长。URL 过期后的重试必须重新签名。

数据库事务与 OBS 写入不能形成分布式事务：先建立文件记录，再执行对象上传，通过状态机、幂等键和补偿任务清理孤儿对象或失败记录。文件删除采用逻辑删除/待清理状态，确认无活动回答引用后再异步删除对象。

问题受理后，`qa_question_file` 是“本次用户消息包含哪些附件、各自承担什么用途”的事实来源。历史消息查询先按会话和 Owner 读取有界消息，再用这些用户消息 ID 批量联查 `qa_question_file` 与 `qa_file`，避免逐条查询和超出消息窗口的无界加载。接口只投影文件 ID、安全文件名、内容类型、大小、本次用途和当前状态；对象键、签名 URL、摘要和内部路径不进入消息契约。附件逻辑删除后仍保留关联和安全元数据，以便刷新后复原原提问并标记文件不可用。

### 7.2 文件安全

- 文件内容、Excel 单元格、OCR 文本和知识片段均视为不可信输入，不能覆盖系统指令。
- 校验文件头与 MIME，不只依赖扩展名；拒绝压缩炸弹、异常页数和超限内容。
- 根据公司要求接入恶意文件检测；检测未通过前不得解析或传给模型。
- OCR/解析日志不得记录完整文件内容。
- 文件访问、签名 URL 生成、解析、删除和模型读取均写审计事件。

## 8. 问答执行流水线

```text
当前问题 + 最近消息 + 持久化会话上下文
                         │
                         ▼
            意图识别、实体提取、参数合并、指代消解
                  ┌──────┴────────┐
                  ▼               ▼
             唯一可执行       缺参数/歧义/低置信度
                  │               └─> 确定性追问并持久化待澄清状态
              ┌───┴───┐
              ▼       ▼
         知识库检索  GoldenDB查询
              └───┬───┘
                  ▼
       标准化、去重、版本判断、字段级冲突检测
         ┌────────┼────────┐
         ▼        ▼        ▼
       一致      冲突     证据不足
         │        │        └─> 澄清或明确拒答，不让模型补事实
         │        └─> 多来源并列展示 + 人工复核提示
         ▼
     受控提示词 + 证据 + 有界上下文 -> HiAgent 流式生成
                         │
                         ▼
                持久化答案、证据与引用
```

第一阶段核心意图包括：

- 产品及交易基础信息：产品经理、投资经理、产品形式、起止日期、费率、单笔交易员等。
- 产品最新信息：最新说明书、费率调整公告、备案通知书、产品分层、费率调整计划等。

意图识别结果必须是结构化对象，至少包含：决策状态、意图、置信度、规范化问题、带类型实体、必填参数、缺失字段、候选项、实体来源消息 ID 和澄清原因。只有 `RESOLVED` 才能进入证据查询。

无法证明“它”“这个产品”等指代对象、存在多个授权候选、缺少必填参数或置信度不足时，系统生成确定性追问，不调用知识库、业务库、对账或回答模型。用户补充后应合并原意图和新参数继续原问题；用户明确提出新问题时应放弃旧追问状态。结构化上下文必须持久化，不能只依赖最近聊天窗口。

## 9. 证据与准确性控制

GoldenDB 和公司知识库统一转换为证据项，至少包含：来源类型、稳定来源 ID、标题、字段名、字段值、业务时间、版本、权限标签、摘要和可展示引用。

确定性对账规则负责：

- 产品代码、交易流水号等主键归一化。
- 日期、费率、金额、人员名称和枚举值标准化。
- “最新”按业务生效时间与文档版本选择，不按检索排序猜测。
- 同字段多来源一致性、时效性和完整性判断。
- 冲突证据完整保留，不让模型自行选择某一个来源为真。

大模型不是事实源。提示词必须要求答案只依据传入证据；证据不足时明确说明无法确认；冲突时展示各来源值、来源和时间，并提示人工审核。引用必须能追溯到本次持久化证据快照，不能仅保存易变 URL。

## 10. 公司 HiAgent 适配

第一阶段按公司运行态 API 适配：

```text
POST {baseUrl}/create_conversation
  {"Inputs":{},"UserID":"..."}
          │
          ▼ AppConversationID
POST {baseUrl}/chat_query
  {"UserID":"...","AppConversationID":"...","Query":"...",
   "ResponseMode":"streaming","PubAgentJump":false}
          │
          ▼ text/event-stream；解析增量并捕获 MessageID
POST {baseUrl}/stop_message
  {"UserID":"...","MessageID":"..."}
```

- `UserID` 按公司文档限制映射为 1～20 个字符的稳定值，不直接暴露不符合格式的认证主体。
- 每次回答生成创建独立的 HiAgent 会话，避免平台会话与本系统长期会话耦合，也便于精确停止和重试。
- `AppConversationID` 按回答保存为 `qa_answer.app_conversation_id`，流事件中的 `MessageID` 保存为 `qa_answer.message_id`；系统会话 ID 和平台会话 ID 不复用。
- `QueryID`、`TaskID`、`TotalTokens`、`Latency`、`TracingJsonStr`、`IntentionJsonStr` 和 `RetrieverResource` 已按平台字段预留回答元数据列，但在真实流事件路径确认前保持为空。
- `Query` 由系统控制的意图、上下文摘要、数据库证据、知识库证据和风险约束组成。
- 一期不向 `QueryExtends.Files` 传递任何文件。后续阶段若重新启用附件，文件对象结构与授权方式必须重新完成真实联调契约测试。
- 重新生成不调用平台 `/query_again`，而是创建新的问题 ID 和回答 ID，只复用原问题文本并重新执行意图校验和双通道查询。新回答通过 `regenerated_from_answer_id` 关联原回答，原问题和原回答继续保留；历史附件不复制到新问题。
- 可选的反馈同步只有在契约确认后启用；本地反馈先可靠落库，再异步同步。

公司文档尚未完整定义流事件字段、结束事件、错误事件、认证头、`Inputs`、文件对象结构及 `MessageID` 层级。未完成真实样例契约测试前，HiAgent 适配器不得标记为生产就绪；解析失败必须失败关闭，不能猜测字段。详细字段边界见 ADR-019。

## 11. 回答、文件与任务状态机

回答目标状态：

```text
PENDING -> UNDERSTANDING -> NEEDS_CLARIFICATION
   └──> RETRIEVING -> RECONCILING -> GENERATING -> COMPLETED
   └──────── 任意执行态 ───────────────> FAILED / INCOMPLETE
   └──────── 任意执行态 ───────────────> CANCEL_REQUESTED
                                               ├─> CANCELLED
                                               └─> CANCEL_FAILED
```

正常完成的答案必须永久保留；重新生成作为新的完整问答轮次追加用户问题和助手回答，并关联原回答，不覆盖、删除或篡改旧问题与旧答案。已输出部分内容后失败标记为 `INCOMPLETE` 并保留已持久化内容。低置信度和歧义属于可恢复的 `NEEDS_CLARIFICATION`，不是依赖故障。

停止接口先在 GoldenDB 事务内条件更新回答为 `CANCEL_REQUESTED`，同时写入停止任务。任务在事务外调用 HiAgent `/stop_message`；生成尚未开始时直接本地停止。所有增量、完成、失败和停止写入都带期望状态条件，保证并发竞争只有一个合法终态。停止成功后保留停止前已经生成并持久化的部分文本，同时明确标记 `CANCELLED`，不把部分文本伪装为完整回答。

## 12. 事务、异步任务与事件可靠性

提交问题的本地事务至少原子写入：用户消息、助手占位消息、回答、问题与文件关联、幂等记录、生成任务和会话更新时间。事务提交后由持久任务工作节点执行外部调用，不能只投递本机线程池。

完成回答的事务至少原子写入：回答终态、助手消息、证据快照、证据项、引用、会话结构化上下文和任务完成状态。失败与追问也必须有明确终态和可审计原因。

任务执行采用租约、心跳、指数退避、有界重试、死信和启动恢复扫描。任何未预期运行时异常都必须被顶层任务边界捕获并转换为可恢复任务或回答终态，不能让回答永久停留在中间状态。

生产事件使用 Redis Stream 或等价持久事件机制：

- 每个回答内事件严格递增并可按 `Last-Event-ID` 重放。
- 跨实例可订阅，有限保留且有容量治理。
- 检测重放缺口；发生缺口时返回明确错误，客户端读取回答快照恢复。
- 慢消费者与断开连接不阻塞生成；浏览器断开不等同于停止回答。
- Redis 故障不丢失 GoldenDB 中的最终回答；恢复后可从快照继续。

当前回答事件已采用“先写 GoldenDB `qa_answer_event`，再通知本实例订阅者”的实现。刷新或重新进入会话时，历史消息会恢复非文本增量的执行阶段和引用产物；活动回答可从持久事件序号重放。实时订阅仍是本实例内通知，生成任务仍由本机执行器投递，因此跨实例即时推送和进程重启后的生成继续执行尚不满足生产要求，仍需 Redis Stream（或等价机制）与持久生成任务。

## 13. 安全与合规

- 正式身份来自公司统一认证，开发模拟用户不得进入测试或生产。
- 按用户、组织、角色和数据权限裁剪知识库、业务库和文件证据。
- 使用只读数据库账号、查询目录和字段白名单，禁止任意 SQL。
- OBS 私有化、短期签名、对象级授权、服务端加密和生命周期清理。
- 不在日志记录完整问题、答案、知识片段、业务字段、OCR 文本、令牌或签名 URL。
- 对提示词注入、越权检索、恶意文件、OCR 欺骗、跨会话引用、重放攻击和模型数据外发建立专项测试。
- 审计记录谁在何时访问了哪个逻辑资源、执行了何种操作及结果，但不重复保存敏感正文。

“不涉及敏感数据”仍需在公司数据分级确认后才能作为正式结论；内部知识库、产品和交易数据默认按受控内部数据处理。

## 14. 可观测性与容量

统一使用 `traceId`、`answerId`、`conversationId`、`taskId`、外部请求 ID 和脱敏用户标识关联日志、指标和审计。至少监控：

- 问答提交 QPS、并发生成数、队列长度、排队时间和各阶段 P50/P95/P99。
- 意图准确率、追问率、指代消解成功率、证据充分率和冲突率。
- 知识库、GoldenDB、OCR、OBS、HiAgent 的成功率、超时率、限流率和重试率。
- 首字延迟、完整回答时延、SSE 断线/重连/重放缺口和停止成功率。
- 文件上传失败、OCR 失败、孤儿对象、任务租约超时和死信数量。
- 有引用回答率、无依据事实率、反馈率和人工复核提示率。

容量验收同时覆盖 10 QPS 问答提交量和 50 个并发用户。生成工作节点、Jetty 长连接、Redis Stream、GoldenDB 连接池和外部平台配额必须联合压测，并保留不少于 30% 的容量余量。生产至少两个副本跨故障域部署，入口网关关闭 SSE 缓冲并设置合理空闲超时和优雅下线策略。

## 15. 部署拓扑

```text
公司网关 / 统一认证
          │
          ▼
       Web 前端
          │
          ▼
智能问答服务（至少 2 副本，Jetty）
   │        │        │        │
   ▼        ▼        ▼        ▼
GoldenDB  Redis   私有 OBS   公司平台
                           ├─ 知识库
                           ├─ OCR
                           └─ HiAgent
```

同一不可变镜像在开发、测试、生产晋级，只替换外部 XML 配置与密钥。容器以非 Root、只读文件系统和最小权限运行；健康检查区分存活、就绪，并把关键外部依赖的降级状态暴露给监控而不是泄露给匿名用户。

## 16. 当前代码基线与目标差距

当前已经具备的基线包括：六边形分层、Jetty、XML 多环境配置、仅限开发环境的服务端模拟用户、测试与生产模拟认证启动保护、提问 Agent 类型识别与 `SMART_DATA` 路由、原生 MyBatis 会话/回答持久化、会话稳定游标分页、活动会话删除保护、两步式问答、GoldenDB 回答事件历史与五阶段白盒执行过程/引用恢复、基础 SSE、停止任务、回答保留、反馈、意图历史传入、一期产品/交易业务语义目录、受控 Query Plan、固定 MyBatis 语句编译与 Owner 条件测试、V6 回答事件历史，以及初步 HiAgent 客户端骨架。历史代码仍保留文件模型和适配器作为后续扩展，但一期配置不注册文件接口，新问题也不接收文件引用。未知意图、非法业务日期、低置信度、无可信来源的指代和实体歧义均在问题理解阶段进入 `NEEDS_CLARIFICATION`，且不调用知识库、业务库、对账或模型。低置信度实体不会进入长期可信上下文，数据库候选可按序号、唯一标识或授权目录返回的规范名称恢复。结构化上下文按实体保存来源用户消息 ID；旧 JSON 缺少逐实体来源时不会使用全局问题 ID 推断。模型输入包含已确认实体及来源消息 ID，未确认的 HiAgent 流契约默认禁止启用，解码器不再递归猜测嵌套字段，SSE 读取具有单行、单事件和累计响应上限。停止任务在租约过期且尝试耗尽时会原子收敛回答与任务为失败。业务语义目录中缺少物理列映射的字段在执行 SQL 前失败关闭。白盒事件只保存流程版本、意图摘要、结果数量、核验状态和阶段时间，不保存提示词、思维链、原始 SQL、内部地址或证据正文。

以下属于目标能力，当前不可视为已经完成：

- 公司统一认证生产适配器、身份声明映射和权限传递；开发模拟用户开关与环境隔离已经完成。
- 公司统一权限驱动的后端 Agent 功能授权；前端目录只控制展示，未开放 Agent 不会发送请求，后端仍须独立拒绝绕过页面的调用。
- 公司知识库真实接口、权限映射和契约测试。
- 数据中台 GoldenDB 真实授权视图、字段口径、独立只读数据源、`EXPLAIN` 证据和字段级对账规则。
- 后续阶段的真实 OBS、安全扫描、Excel 查询输入解析、文本解析、扫描 PDF/图片 OCR、短期签名 URL、持久处理任务与生命周期清理。
- 后续阶段的文件角色识别、查询参数与证据隔离、结构化文件证据快照、引用和冲突展示。
- 经授权实体目录验证的完整指代消解、歧义追问、补参恢复和跨服务重启端到端验证；当前已具备逐实体来源、旧上下文失败关闭及本地持久仓储恢复测试。
- 持久化生成任务/Outbox 与跨实例 Redis Stream；回答事件和展示快照已持久化，但跨实例即时推送及任务重启续跑尚未完成。
- HiAgent 真实 SSE、认证、文件参数、停止和反馈联调。
- 10 QPS、50 并发、安全、灾备、监控告警和生产环境验收。

## 17. 建设顺序与依赖

1. 冻结一期范围、外部系统责任和待确认清单。
2. 冻结统一认证、Agent 类型、会话、问答、SSE、停止和反馈 API 契约。
3. 以新迁移补齐幂等、任务、证据、身份映射和审计模型。
4. 接入公司统一认证适配器；严格隔离的开发模拟用户适配器已经完成。
5. 完成公司知识库与 GoldenDB 受控查询的真实适配器。
6. 完成结构化理解、指代消解、歧义追问、补参恢复和双来源对账。
7. 完成 HiAgent 真实流式、停止、反馈和失败语义。
8. 完成持久任务、Redis Stream、快照恢复、监控、审计和安全加固。
9. 完成前后端联调、准确性评测、10 QPS/50 并发压测和生产就绪验收。

每一步都必须以自动化测试、契约样例和可追溯验收记录作为完成条件；外部契约未确认时只允许保留端口和测试替身，不得伪造生产实现。

## 18. 后续阶段隔离

合同比对、申赎确认单核对和合同智能审核属于后续阶段，应分别建立领域边界、规则模型、文件处理策略、CISP 契约和 ADR。一期只展示这些 Agent，不把相关业务逻辑提前耦合进智能问答核心。可复用的知识检索、文件解析、证据标准化和模型访问能力保留独立端口，为未来拆分通用 RAG/AI 能力服务提供边界，但一期不因预设微服务而增加分布式复杂度。
