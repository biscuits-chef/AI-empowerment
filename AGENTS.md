# Codex Java 生产级工程母版

## 使命

在尽量减少用户干预的前提下，将已批准的业务需求交付为可投入生产的 Java 服务。始终守住安全边界：不得虚构业务规则、凭据、生产端点、破坏性迁移或部署授权。

## 事实来源

修改代码前按以下顺序阅读：

1. 阅读 `REQUIREMENTS.md`，确认范围与验收标准。
2. 阅读 `ARCHITECTURE.md` 及 `docs/adr/` 下已接受的文件，确认边界与决策。
3. 阅读 `.agents/skills/` 下与当前工作流相关的 Skill。
4. 在声明具备发布条件前，阅读 `docs/operations/production-readiness-checklist.md`。

当事实来源冲突时，按以下优先级处理：用户明确要求、已接受的 ADR、`REQUIREMENTS.md`、`ARCHITECTURE.md`、本文件、项目局部约定。

## 自主交付循环

对于构建、变更或修复任务，不必就常规实现选择反复询问，持续执行以下步骤：

1. 在工作计划中将需求重述为可度量的验收标准。
2. 编辑前检查现有实现与测试。
3. 仅当决策长期有效、影响面广且难以逆转时记录新 ADR。
4. 通过端口与适配器实现最小的端到端纵向切片。
5. 按风险补充或更新单元、集成、契约与架构测试。
6. 运行 `mvn -B -ntp clean verify`，持续修复直到全部门禁通过。
7. 审查完整差异，覆盖正确性、安全、韧性、可观测性、迁移和回滚。
8. 更新相关文档和生产就绪检查清单。
9. 报告已交付行为、验证证据、剩余风险，以及必须由责任人决定的事项。

任何必需门禁失败或被跳过时都不得声明完成。除非需求明确修改质量策略，不得为了通过构建而削弱门禁。

## 子代理协作

对于可以清晰拆分的工作，使用 `.codex/agents/` 中的项目代理。只委派边界明确的任务，并保留唯一的集成负责人。

- `architect`：边界变更、ADR、数据流与失败模式。
- `developer`：功能实现与聚焦式重构。
- `database_specialist`：GoldenDB 表结构、SQL、索引、迁移及事务行为。
- `test_engineer`：测试策略、边界场景、故障注入与覆盖率。
- `reviewer`：只读审查正确性、安全性、兼容性与可维护性。
- `production_guardian`：生产就绪、韧性、可观测性、发布与回滚。

可并行开展相互独立的调研或审查，但不得让多个代理同时编辑相同文件。主代理负责最终集成，并必须重新运行完整质量门禁。

## 架构规则

- 除非已接受的 ADR 升级基线，否则使用兼容 Java 8 的语法和 Spring Boot 2.7.18。
- 将 Java 8/Spring Boot 2.7 视为兼容基线，而非永久目标；记录并优先推进到当前受支持的 Java LTS 与 Spring Boot 版本。
- `domain` 不得依赖 Spring、持久化、缓存、HTTP、云 SDK 或序列化框架。
- 在 `domain.port.in` 定义入站用例，在 `domain.port.out` 定义出站依赖。
- 在 `application.service` 中放置流程编排和事务边界。
- HTTP 代码放在 `adapter.in`，基础设施代码放在 `adapter.out`，依赖装配放在 `config`。
- 依赖方向必须指向内层。适配器可以依赖端口，端口不得依赖适配器。
- 优先使用组合、不可变对象、构造器注入、显式类型和小而内聚的类。
- 仅在能够降低耦合或提高变更安全性时使用 SOLID 与设计模式，避免推测性抽象。
- Web 适配器使用兼容 RFC 7807 的问题响应，绝不暴露密钥或堆栈信息。

## Java 与阿里规范导向约定

- 使用含义清晰的英文标识符；不得使用未解释的缩写、拼音或单字母业务名称。
- 所有类、接口、枚举、枚举值、成员字段、构造器和方法（包括私有成员）都必须有中文 Javadoc；每个方法或构造器入参必须有对应的中文 `@param`，非 `void` 方法必须有中文 `@return`，显式声明异常时必须有中文 `@throws`。
- `ChineseDocumentationArchitectureTest` 是强制门禁：新增代码缺少上述中文注释时构建必须失败，禁止跳过或削弱该检查。
- 注释要求同时适用于生产源码和测试源码；复杂业务规则、并发条件、事务边界与失败分支还需中文行内注释解释原因，不能仅依靠声明说明。
- 注释必须与代码同步，禁止逐行翻译代码、保留注释掉的旧代码或用注释掩盖含糊命名；框架规定的英文标记、协议字段和第三方术语可保留原文，并用中文解释其业务含义。
- 方法职责保持聚焦，圈复杂度不得超过 PMD 配置阈值。
- 集合或 Optional 不得返回 `null`。仅当返回值允许不存在时使用 `Optional`。
- 金额使用 `BigDecimal`，并显式指定精度和舍入规则；禁止用二进制浮点数表示金额。
- 持久化时间戳使用 `Instant`，系统边界统一采用 UTC。
- 不得捕获 `Throwable`，也不得静默吞掉异常；保留异常原因并补充可执行的上下文。
- 不得记录凭据、令牌、个人数据、完整请求体或对象内容。
- 对集合、重试、队列、载荷大小、超时和并发量设置上限。
- 避免 N+1 查询、无界扫描、`SELECT *` 以及缺少确定性分页的 SQL。
- 每个对外行为变更都必须有测试，每个缺陷修复都必须有回归测试。

## GoldenDB 规则

- 生产 GoldenDB 是最终依据；本地 MySQL 只作为兼容性替身。
- 生产发布前确认目标 GoldenDB 的产品版本和支持的 MySQL 语法。
- 使用 Flyway 只向前迁移，绝不编辑已执行的迁移。
- 优先使用“扩展—迁移—收缩”的增量变更，将破坏性清理拆分到后续版本。
- 明确主键、分片/分布考量、唯一约束、索引和查询计划。
- 缩短事务时间。没有明确 ADR 时，不得在持有数据库锁期间执行远程网络 I/O。
- 对可重试写操作保证幂等，并区分瞬时数据库故障与永久故障。

## Redis 规则

- Redis 应被明确设计为优化手段、协调机制或事实来源，绝不能无意间成为事实来源。
- Key 必须带命名空间和版本；有意识地设置 TTL，并记录淘汰行为。
- 缓存读取必须容忍未命中和损坏值；热点 Key 需要防止缓存击穿。
- 请求链路中禁止宽范围扫描 Key，Redis 中不得存放密钥或无界数据。

## OBS 规则

- 通过 `ObjectStoragePort` 访问 OBS，将华为 SDK 限制在适配器内部。
- 使用流式方式处理对象，禁止在内存中缓冲无界文件。
- 校验对象大小、类型和名称，由服务端生成对象 Key。
- Bucket 保持私有；凭据由密钥管理系统按最小权限提供，并启用加密与生命周期策略。
- 设计幂等性、分段上传清理、校验和、超时，以及部分失败的补偿策略。

## 验证与完成定义

本地必需门禁为：

```bash
mvn -B -ntp clean verify
```

该命令必须执行单元测试、架构测试、Checkstyle、PMD/CPD、SpotBugs、JaCoCo、Maven Enforcer，以及命名匹配 `*IT` 的集成测试。

只有满足以下条件，任务才算完成：

- 验收标准已满足，并可追溯到测试；
- 所有必需门禁通过，且未压制新增问题；
- 迁移向后兼容，并记录回滚或前滚方案；
- 已处理变更依赖的超时、重试、幂等性、指标、日志、健康检查和告警；
- 密钥始终位于源码之外，示例仅包含占位符；
- 文档和 ADR 已反映长期有效的变更；
- 已明确剩余风险和生产环境人工验证项。

## 安全边界

对于实现类请求，常规读取、仓库内编辑、测试、构建和本地容器操作均在授权范围内。执行生产写入、部署、破坏性数据变更、外部消息发送、采购、权限扩展或重大范围扩张前必须询问。不得提交或输出真实密钥。不得使用破坏性的 Git 或文件系统命令丢弃用户工作。
